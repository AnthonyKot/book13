# ch2 — Errors: Failure Is Part of the Return Value

**Verified.** Reference solution (`wasm-compiler/shims/testdata/solutions/ch2.go`, `return "", errUserNotFound` in the unknown-ID branch) passes all 4 hidden tests in yaegi (`TestAllLabSolutions/ch2`) and under gc 1.22 (`go vet` + `go run`: `User: Alice` / `Lookup failed: user not found`). Starter still fails 3 tests. Prediction checked with `go run`: the panicking version prints `before`, then `panic: user 2 not found` plus a goroutine trace on stderr, exit status 2; a panic in a non-main goroutine also terminates the whole program (checked). Starter is not solvable by deleting a line (dropping the `return` makes ID 2 return Alice).

**Changed.** Correct-option explanation now states the observable facts (stderr trace, exit status 2, later prints never happen); option 3 says “any goroutine, not only main”. Lesson: “the program terminates” → “terminates with exit status 2, whichever goroutine panicked”, plus a paragraph on checking `err` before the value, sentinels, `errors.Is` and `%w`. Two FAIL messages now say what to do: “Return an empty name and the user-not-found error for an unknown ID.” → names the `errUserNotFound` sentinel and its message; “Keep both the success value and returned error usable by the caller.” → states the two exact strings and “change GetUser, not userLabel”. Hints rewritten to escalate; hint 3 previously quoted the fix verbatim (“Use the existing sentinel value: `return "", errUserNotFound`”).

**Unsettled.** The test compares `err.Error()` to the exact string, so a wrapped error fails `missing user error` even though the lesson recommends wrapping; kept, because the challenge text asks for the sentinel itself.
