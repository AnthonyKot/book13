# ch11 — A Deadline Is a Signal

**Verified.** Reference solution (`WithTimeout(parent, timeout)`, `defer cancel()`, `fetch(ctx)`) passes all 4 hidden tests in yaegi and, with the tests embedded in a scratch `main`, under `go run -race` with go1.22.2. Prediction re-run with gc: prints `done` at 100 ms while `ctx.Err()` is already `DeadlineExceeded`, so "after about 100 ms" is the only correct option. Starter fails exactly `parent cancellation` and `early cleanup`; not fixable by deleting a line. `go vet` confirmed: it flags `ctx, _ := WithTimeout` and a return path missing `cancel`, but is silent on the starter's `_ = cancel`.

**Changed.**
- Hints rewritten to escalate. Old hint 1 was the fix itself ("Call `context.WithTimeout(parent, timeout)`…"); now 1 points at the two decisive spots, 2 states the derivation-and-cancel rule, 3 gives the three-line shape.
- Lesson gained one paragraph on *why* cancel matters (parent retains the child, timer stays armed until the deadline) and what vet does and does not catch — the "fast call skips cancel" case the `early cleanup` test exercises had no explanation in the lesson.

**Unsettled.** Nothing. Tests are deterministic: `deadline signal` blocks on `Done()` rather than racing a sleep, so no scheduling luck is involved.
