# ch10 — Sub-slices: Small Views Can Retain Large Storage

**Verified.** Reference solution (`wasm-compiler/shims/testdata/solutions/ch10.go`, `make` + `copy` with the starter's limit) passes all 4 hidden tests in yaegi (`TestAllLabSolutions/ch10`) and under gc 1.22 (`go vet` + `go run`: `abcdefghij`). Starter still fails `source independence`, `result independence` and `short input`. Prediction claim measured with `runtime.MemStats` under gc: a 10 MB array stays fully on the heap after `runtime.GC()` while a ten-byte view of it is live and `data` is nil; 0 MB once the view is dropped. The outside reviewer's verdict (lab is solid) holds: tests check both directions of aliasing plus short and nil input, and the starter is not solvable by deleting a line.

**Finding — yaegi and gc disagree.** `bytes.Clone` (Go 1.20) runs in yaegi 0.16.1; the generic `slices.Clone` (Go 1.21) does not (“undefined type for E”). The lesson now names both alternatives, dates them, and says which one the browser evaluator runs. Also noted from `go run`: both Clone helpers are `append` underneath, so a ten-byte clone has capacity 16.

**Changed.** Correct-option explanation now carries the measured numbers. Lesson adds how much `copy` transfers and the Clone paragraph. Two FAIL messages now say what to change (“The source and returned header must use independent backing storage.” → “Writing header[1] changed source[1], so both still share one array. Return storage you allocated, not a reslice of the input.”). Hints rewritten to escalate; hints 2 and 3 previously quoted the code (“`header := make([]byte, limit)`”, “Use `copy(header, data[:limit])`, then return `header`”).

**Unsettled.** Nothing.
